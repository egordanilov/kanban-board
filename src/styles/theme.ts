export interface ThemeType {
    color: {
        background: string;
        fontColor: string;
    }
}

export const theme = {
    color: {
        background: 'red',
        fontColor: 'white',
    }
}
